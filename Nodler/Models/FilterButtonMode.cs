namespace Nodler;

public enum FilterButtonMode
{
    /// <summary>Tabs are generated per category; color comes from each category's first node.</summary>
    Auto,
    /// <summary>Tabs come from the user-supplied FiltersButtonsConfigs list.</summary>
    Manual
}
